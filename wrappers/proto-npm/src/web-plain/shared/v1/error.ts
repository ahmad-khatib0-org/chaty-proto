// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.10.0
//   protoc               unknown
// source: shared/v1/error.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "shared.v1";

export interface AppError {
  id: string;
  /** displayed to the end user without debugging info */
  message: string;
  /** Internal debug info */
  detailedError: string;
  /** grpc status code */
  statusCode: number;
  /** Code path or func name */
  location: string;
  /** If false, i18n may not apply */
  skipTranslation?: boolean | undefined;
  errors: { [key: string]: string };
}

export interface AppError_ErrorsEntry {
  key: string;
  value: string;
}

function createBaseAppError(): AppError {
  return {
    id: "",
    message: "",
    detailedError: "",
    statusCode: 0,
    location: "",
    skipTranslation: undefined,
    errors: {},
  };
}

export const AppError: MessageFns<AppError> = {
  encode(message: AppError, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    if (message.detailedError !== "") {
      writer.uint32(26).string(message.detailedError);
    }
    if (message.statusCode !== 0) {
      writer.uint32(40).uint32(message.statusCode);
    }
    if (message.location !== "") {
      writer.uint32(50).string(message.location);
    }
    if (message.skipTranslation !== undefined) {
      writer.uint32(56).bool(message.skipTranslation);
    }
    Object.entries(message.errors).forEach(([key, value]) => {
      AppError_ErrorsEntry.encode({ key: key as any, value }, writer.uint32(66).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AppError {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAppError();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.detailedError = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.statusCode = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.location = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.skipTranslation = reader.bool();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          const entry8 = AppError_ErrorsEntry.decode(reader, reader.uint32());
          if (entry8.value !== undefined) {
            message.errors[entry8.key] = entry8.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AppError {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      detailedError: isSet(object.detailedError) ? globalThis.String(object.detailedError) : "",
      statusCode: isSet(object.statusCode) ? globalThis.Number(object.statusCode) : 0,
      location: isSet(object.location) ? globalThis.String(object.location) : "",
      skipTranslation: isSet(object.skipTranslation) ? globalThis.Boolean(object.skipTranslation) : undefined,
      errors: isObject(object.errors)
        ? Object.entries(object.errors).reduce<{ [key: string]: string }>((acc, [key, value]) => {
          acc[key] = String(value);
          return acc;
        }, {})
        : {},
    };
  },

  toJSON(message: AppError): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.detailedError !== "") {
      obj.detailedError = message.detailedError;
    }
    if (message.statusCode !== 0) {
      obj.statusCode = Math.round(message.statusCode);
    }
    if (message.location !== "") {
      obj.location = message.location;
    }
    if (message.skipTranslation !== undefined) {
      obj.skipTranslation = message.skipTranslation;
    }
    if (message.errors) {
      const entries = Object.entries(message.errors);
      if (entries.length > 0) {
        obj.errors = {};
        entries.forEach(([k, v]) => {
          obj.errors[k] = v;
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AppError>, I>>(base?: I): AppError {
    return AppError.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AppError>, I>>(object: I): AppError {
    const message = createBaseAppError();
    message.id = object.id ?? "";
    message.message = object.message ?? "";
    message.detailedError = object.detailedError ?? "";
    message.statusCode = object.statusCode ?? 0;
    message.location = object.location ?? "";
    message.skipTranslation = object.skipTranslation ?? undefined;
    message.errors = Object.entries(object.errors ?? {}).reduce<{ [key: string]: string }>((acc, [key, value]) => {
      if (value !== undefined) {
        acc[key] = globalThis.String(value);
      }
      return acc;
    }, {});
    return message;
  },
};

function createBaseAppError_ErrorsEntry(): AppError_ErrorsEntry {
  return { key: "", value: "" };
}

export const AppError_ErrorsEntry: MessageFns<AppError_ErrorsEntry> = {
  encode(message: AppError_ErrorsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AppError_ErrorsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAppError_ErrorsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AppError_ErrorsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: AppError_ErrorsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AppError_ErrorsEntry>, I>>(base?: I): AppError_ErrorsEntry {
    return AppError_ErrorsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AppError_ErrorsEntry>, I>>(object: I): AppError_ErrorsEntry {
    const message = createBaseAppError_ErrorsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
