// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.10.0
//   protoc               unknown
// source: service/v1/config.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "service.v1";

/** Chaty API Configuration */
export interface ChatyConfig {
  /** Chaty API Version */
  chatyVersion: string;
  /** Features enabled on this Chaty node */
  features?:
    | ChatyFeatures
    | undefined;
  /** WebSocket URL */
  ws: string;
  /** URL pointing to the client serving this node */
  app: string;
  /** Web Push VAPID public key */
  vapid: string;
  /** Build information */
  build?: BuildInformation | undefined;
}

/** Feature Configuration */
export interface ChatyFeatures {
  /** hCaptcha configuration */
  captcha?:
    | CaptchaFeature
    | undefined;
  /** Whether email verification is enabled */
  email: boolean;
  /** Whether this server is invite only */
  inviteOnly: boolean;
  /** File server service configuration */
  files?:
    | Feature
    | undefined;
  /** Proxy service configuration */
  proxy?:
    | Feature
    | undefined;
  /** Voice server configuration */
  livekit?: VoiceFeature | undefined;
}

/** hCaptcha Configuration */
export interface CaptchaFeature {
  /** Whether captcha is enabled */
  enabled: boolean;
  /** Client key used for solving captcha */
  key: string;
}

/** Generic Service Configuration */
export interface Feature {
  /** Whether the service is enabled */
  enabled: boolean;
  /** URL pointing to the service */
  url: string;
}

/** Voice Server Configuration */
export interface VoiceFeature {
  /** Whether voice is enabled */
  enabled: boolean;
  /** All livekit nodes */
  nodes: VoiceNode[];
}

/** Information about a livekit node */
export interface VoiceNode {
  name: string;
  /** Format: double */
  lat: number;
  /** Format: double */
  lon: number;
  publicUrl: string;
}

/** Build Information */
export interface BuildInformation {
  /** Commit Hash */
  commitSha: string;
  /** Commit Timestamp */
  commitTimestamp: string;
  /** Git Semver */
  semver: string;
  /** Git Origin URL */
  originUrl: string;
  /** Build Timestamp */
  timestamp: string;
}

function createBaseChatyConfig(): ChatyConfig {
  return { chatyVersion: "", features: undefined, ws: "", app: "", vapid: "", build: undefined };
}

export const ChatyConfig: MessageFns<ChatyConfig> = {
  encode(message: ChatyConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.chatyVersion !== "") {
      writer.uint32(10).string(message.chatyVersion);
    }
    if (message.features !== undefined) {
      ChatyFeatures.encode(message.features, writer.uint32(18).fork()).join();
    }
    if (message.ws !== "") {
      writer.uint32(26).string(message.ws);
    }
    if (message.app !== "") {
      writer.uint32(34).string(message.app);
    }
    if (message.vapid !== "") {
      writer.uint32(42).string(message.vapid);
    }
    if (message.build !== undefined) {
      BuildInformation.encode(message.build, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ChatyConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseChatyConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.chatyVersion = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.features = ChatyFeatures.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.ws = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.app = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.vapid = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.build = BuildInformation.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ChatyConfig {
    return {
      chatyVersion: isSet(object.chatyVersion) ? globalThis.String(object.chatyVersion) : "",
      features: isSet(object.features) ? ChatyFeatures.fromJSON(object.features) : undefined,
      ws: isSet(object.ws) ? globalThis.String(object.ws) : "",
      app: isSet(object.app) ? globalThis.String(object.app) : "",
      vapid: isSet(object.vapid) ? globalThis.String(object.vapid) : "",
      build: isSet(object.build) ? BuildInformation.fromJSON(object.build) : undefined,
    };
  },

  toJSON(message: ChatyConfig): unknown {
    const obj: any = {};
    if (message.chatyVersion !== "") {
      obj.chatyVersion = message.chatyVersion;
    }
    if (message.features !== undefined) {
      obj.features = ChatyFeatures.toJSON(message.features);
    }
    if (message.ws !== "") {
      obj.ws = message.ws;
    }
    if (message.app !== "") {
      obj.app = message.app;
    }
    if (message.vapid !== "") {
      obj.vapid = message.vapid;
    }
    if (message.build !== undefined) {
      obj.build = BuildInformation.toJSON(message.build);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ChatyConfig>, I>>(base?: I): ChatyConfig {
    return ChatyConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ChatyConfig>, I>>(object: I): ChatyConfig {
    const message = createBaseChatyConfig();
    message.chatyVersion = object.chatyVersion ?? "";
    message.features = (object.features !== undefined && object.features !== null)
      ? ChatyFeatures.fromPartial(object.features)
      : undefined;
    message.ws = object.ws ?? "";
    message.app = object.app ?? "";
    message.vapid = object.vapid ?? "";
    message.build = (object.build !== undefined && object.build !== null)
      ? BuildInformation.fromPartial(object.build)
      : undefined;
    return message;
  },
};

function createBaseChatyFeatures(): ChatyFeatures {
  return {
    captcha: undefined,
    email: false,
    inviteOnly: false,
    files: undefined,
    proxy: undefined,
    livekit: undefined,
  };
}

export const ChatyFeatures: MessageFns<ChatyFeatures> = {
  encode(message: ChatyFeatures, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.captcha !== undefined) {
      CaptchaFeature.encode(message.captcha, writer.uint32(10).fork()).join();
    }
    if (message.email !== false) {
      writer.uint32(16).bool(message.email);
    }
    if (message.inviteOnly !== false) {
      writer.uint32(24).bool(message.inviteOnly);
    }
    if (message.files !== undefined) {
      Feature.encode(message.files, writer.uint32(34).fork()).join();
    }
    if (message.proxy !== undefined) {
      Feature.encode(message.proxy, writer.uint32(42).fork()).join();
    }
    if (message.livekit !== undefined) {
      VoiceFeature.encode(message.livekit, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ChatyFeatures {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseChatyFeatures();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.captcha = CaptchaFeature.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.email = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.inviteOnly = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.files = Feature.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.proxy = Feature.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.livekit = VoiceFeature.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ChatyFeatures {
    return {
      captcha: isSet(object.captcha) ? CaptchaFeature.fromJSON(object.captcha) : undefined,
      email: isSet(object.email) ? globalThis.Boolean(object.email) : false,
      inviteOnly: isSet(object.inviteOnly) ? globalThis.Boolean(object.inviteOnly) : false,
      files: isSet(object.files) ? Feature.fromJSON(object.files) : undefined,
      proxy: isSet(object.proxy) ? Feature.fromJSON(object.proxy) : undefined,
      livekit: isSet(object.livekit) ? VoiceFeature.fromJSON(object.livekit) : undefined,
    };
  },

  toJSON(message: ChatyFeatures): unknown {
    const obj: any = {};
    if (message.captcha !== undefined) {
      obj.captcha = CaptchaFeature.toJSON(message.captcha);
    }
    if (message.email !== false) {
      obj.email = message.email;
    }
    if (message.inviteOnly !== false) {
      obj.inviteOnly = message.inviteOnly;
    }
    if (message.files !== undefined) {
      obj.files = Feature.toJSON(message.files);
    }
    if (message.proxy !== undefined) {
      obj.proxy = Feature.toJSON(message.proxy);
    }
    if (message.livekit !== undefined) {
      obj.livekit = VoiceFeature.toJSON(message.livekit);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ChatyFeatures>, I>>(base?: I): ChatyFeatures {
    return ChatyFeatures.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ChatyFeatures>, I>>(object: I): ChatyFeatures {
    const message = createBaseChatyFeatures();
    message.captcha = (object.captcha !== undefined && object.captcha !== null)
      ? CaptchaFeature.fromPartial(object.captcha)
      : undefined;
    message.email = object.email ?? false;
    message.inviteOnly = object.inviteOnly ?? false;
    message.files = (object.files !== undefined && object.files !== null)
      ? Feature.fromPartial(object.files)
      : undefined;
    message.proxy = (object.proxy !== undefined && object.proxy !== null)
      ? Feature.fromPartial(object.proxy)
      : undefined;
    message.livekit = (object.livekit !== undefined && object.livekit !== null)
      ? VoiceFeature.fromPartial(object.livekit)
      : undefined;
    return message;
  },
};

function createBaseCaptchaFeature(): CaptchaFeature {
  return { enabled: false, key: "" };
}

export const CaptchaFeature: MessageFns<CaptchaFeature> = {
  encode(message: CaptchaFeature, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.enabled !== false) {
      writer.uint32(8).bool(message.enabled);
    }
    if (message.key !== "") {
      writer.uint32(18).string(message.key);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CaptchaFeature {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCaptchaFeature();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.enabled = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.key = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CaptchaFeature {
    return {
      enabled: isSet(object.enabled) ? globalThis.Boolean(object.enabled) : false,
      key: isSet(object.key) ? globalThis.String(object.key) : "",
    };
  },

  toJSON(message: CaptchaFeature): unknown {
    const obj: any = {};
    if (message.enabled !== false) {
      obj.enabled = message.enabled;
    }
    if (message.key !== "") {
      obj.key = message.key;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CaptchaFeature>, I>>(base?: I): CaptchaFeature {
    return CaptchaFeature.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CaptchaFeature>, I>>(object: I): CaptchaFeature {
    const message = createBaseCaptchaFeature();
    message.enabled = object.enabled ?? false;
    message.key = object.key ?? "";
    return message;
  },
};

function createBaseFeature(): Feature {
  return { enabled: false, url: "" };
}

export const Feature: MessageFns<Feature> = {
  encode(message: Feature, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.enabled !== false) {
      writer.uint32(8).bool(message.enabled);
    }
    if (message.url !== "") {
      writer.uint32(18).string(message.url);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Feature {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFeature();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.enabled = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.url = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Feature {
    return {
      enabled: isSet(object.enabled) ? globalThis.Boolean(object.enabled) : false,
      url: isSet(object.url) ? globalThis.String(object.url) : "",
    };
  },

  toJSON(message: Feature): unknown {
    const obj: any = {};
    if (message.enabled !== false) {
      obj.enabled = message.enabled;
    }
    if (message.url !== "") {
      obj.url = message.url;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Feature>, I>>(base?: I): Feature {
    return Feature.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Feature>, I>>(object: I): Feature {
    const message = createBaseFeature();
    message.enabled = object.enabled ?? false;
    message.url = object.url ?? "";
    return message;
  },
};

function createBaseVoiceFeature(): VoiceFeature {
  return { enabled: false, nodes: [] };
}

export const VoiceFeature: MessageFns<VoiceFeature> = {
  encode(message: VoiceFeature, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.enabled !== false) {
      writer.uint32(8).bool(message.enabled);
    }
    for (const v of message.nodes) {
      VoiceNode.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VoiceFeature {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVoiceFeature();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.enabled = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.nodes.push(VoiceNode.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VoiceFeature {
    return {
      enabled: isSet(object.enabled) ? globalThis.Boolean(object.enabled) : false,
      nodes: globalThis.Array.isArray(object?.nodes) ? object.nodes.map((e: any) => VoiceNode.fromJSON(e)) : [],
    };
  },

  toJSON(message: VoiceFeature): unknown {
    const obj: any = {};
    if (message.enabled !== false) {
      obj.enabled = message.enabled;
    }
    if (message.nodes?.length) {
      obj.nodes = message.nodes.map((e) => VoiceNode.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VoiceFeature>, I>>(base?: I): VoiceFeature {
    return VoiceFeature.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VoiceFeature>, I>>(object: I): VoiceFeature {
    const message = createBaseVoiceFeature();
    message.enabled = object.enabled ?? false;
    message.nodes = object.nodes?.map((e) => VoiceNode.fromPartial(e)) || [];
    return message;
  },
};

function createBaseVoiceNode(): VoiceNode {
  return { name: "", lat: 0, lon: 0, publicUrl: "" };
}

export const VoiceNode: MessageFns<VoiceNode> = {
  encode(message: VoiceNode, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.lat !== 0) {
      writer.uint32(17).double(message.lat);
    }
    if (message.lon !== 0) {
      writer.uint32(25).double(message.lon);
    }
    if (message.publicUrl !== "") {
      writer.uint32(34).string(message.publicUrl);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VoiceNode {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVoiceNode();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.lat = reader.double();
          continue;
        }
        case 3: {
          if (tag !== 25) {
            break;
          }

          message.lon = reader.double();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.publicUrl = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VoiceNode {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      lat: isSet(object.lat) ? globalThis.Number(object.lat) : 0,
      lon: isSet(object.lon) ? globalThis.Number(object.lon) : 0,
      publicUrl: isSet(object.publicUrl) ? globalThis.String(object.publicUrl) : "",
    };
  },

  toJSON(message: VoiceNode): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.lat !== 0) {
      obj.lat = message.lat;
    }
    if (message.lon !== 0) {
      obj.lon = message.lon;
    }
    if (message.publicUrl !== "") {
      obj.publicUrl = message.publicUrl;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VoiceNode>, I>>(base?: I): VoiceNode {
    return VoiceNode.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VoiceNode>, I>>(object: I): VoiceNode {
    const message = createBaseVoiceNode();
    message.name = object.name ?? "";
    message.lat = object.lat ?? 0;
    message.lon = object.lon ?? 0;
    message.publicUrl = object.publicUrl ?? "";
    return message;
  },
};

function createBaseBuildInformation(): BuildInformation {
  return { commitSha: "", commitTimestamp: "", semver: "", originUrl: "", timestamp: "" };
}

export const BuildInformation: MessageFns<BuildInformation> = {
  encode(message: BuildInformation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.commitSha !== "") {
      writer.uint32(10).string(message.commitSha);
    }
    if (message.commitTimestamp !== "") {
      writer.uint32(18).string(message.commitTimestamp);
    }
    if (message.semver !== "") {
      writer.uint32(26).string(message.semver);
    }
    if (message.originUrl !== "") {
      writer.uint32(34).string(message.originUrl);
    }
    if (message.timestamp !== "") {
      writer.uint32(42).string(message.timestamp);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BuildInformation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBuildInformation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.commitSha = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.commitTimestamp = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.semver = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.originUrl = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.timestamp = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BuildInformation {
    return {
      commitSha: isSet(object.commitSha) ? globalThis.String(object.commitSha) : "",
      commitTimestamp: isSet(object.commitTimestamp) ? globalThis.String(object.commitTimestamp) : "",
      semver: isSet(object.semver) ? globalThis.String(object.semver) : "",
      originUrl: isSet(object.originUrl) ? globalThis.String(object.originUrl) : "",
      timestamp: isSet(object.timestamp) ? globalThis.String(object.timestamp) : "",
    };
  },

  toJSON(message: BuildInformation): unknown {
    const obj: any = {};
    if (message.commitSha !== "") {
      obj.commitSha = message.commitSha;
    }
    if (message.commitTimestamp !== "") {
      obj.commitTimestamp = message.commitTimestamp;
    }
    if (message.semver !== "") {
      obj.semver = message.semver;
    }
    if (message.originUrl !== "") {
      obj.originUrl = message.originUrl;
    }
    if (message.timestamp !== "") {
      obj.timestamp = message.timestamp;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BuildInformation>, I>>(base?: I): BuildInformation {
    return BuildInformation.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BuildInformation>, I>>(object: I): BuildInformation {
    const message = createBaseBuildInformation();
    message.commitSha = object.commitSha ?? "";
    message.commitTimestamp = object.commitTimestamp ?? "";
    message.semver = object.semver ?? "";
    message.originUrl = object.originUrl ?? "";
    message.timestamp = object.timestamp ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
