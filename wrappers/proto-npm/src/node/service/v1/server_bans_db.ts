// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.10.0
//   protoc               unknown
// source: service/v1/server_bans_db.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "service.v1";

export interface ServerBan {
  serverId: string;
  userId: string;
  reason: string;
  bannedBy: string;
  bannedAt: number;
}

function createBaseServerBan(): ServerBan {
  return { serverId: "", userId: "", reason: "", bannedBy: "", bannedAt: 0 };
}

export const ServerBan: MessageFns<ServerBan> = {
  encode(message: ServerBan, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.serverId !== "") {
      writer.uint32(10).string(message.serverId);
    }
    if (message.userId !== "") {
      writer.uint32(18).string(message.userId);
    }
    if (message.reason !== "") {
      writer.uint32(26).string(message.reason);
    }
    if (message.bannedBy !== "") {
      writer.uint32(34).string(message.bannedBy);
    }
    if (message.bannedAt !== 0) {
      writer.uint32(40).int64(message.bannedAt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServerBan {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServerBan();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.serverId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.bannedBy = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.bannedAt = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServerBan {
    return {
      serverId: isSet(object.serverId) ? globalThis.String(object.serverId) : "",
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
      bannedBy: isSet(object.bannedBy) ? globalThis.String(object.bannedBy) : "",
      bannedAt: isSet(object.bannedAt) ? globalThis.Number(object.bannedAt) : 0,
    };
  },

  toJSON(message: ServerBan): unknown {
    const obj: any = {};
    if (message.serverId !== "") {
      obj.serverId = message.serverId;
    }
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    if (message.bannedBy !== "") {
      obj.bannedBy = message.bannedBy;
    }
    if (message.bannedAt !== 0) {
      obj.bannedAt = Math.round(message.bannedAt);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ServerBan>, I>>(base?: I): ServerBan {
    return ServerBan.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ServerBan>, I>>(object: I): ServerBan {
    const message = createBaseServerBan();
    message.serverId = object.serverId ?? "";
    message.userId = object.userId ?? "";
    message.reason = object.reason ?? "";
    message.bannedBy = object.bannedBy ?? "";
    message.bannedAt = object.bannedAt ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
