// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.10.0
//   protoc               unknown
// source: service/v1/search.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { AppError } from "../../shared/v1/error.js";
import { MessageSort, messageSortFromJSON, messageSortToJSON } from "./messages.js";
import { Message } from "./messages_db.js";
import { ServerMember } from "./server_members_db.js";
import { APIUser } from "./users.js";

export const protobufPackage = "service.v1";

export interface SearchUsernamesRequest {
  query: string;
  limit: number;
}

export interface SearchUsernamesResponse {
  data?: SearchUsernamesResponseData | undefined;
  error?: AppError | undefined;
}

export interface SearchUsernamesResponseData {
  users: SearchUser[];
}

export interface SearchUser {
  id: string;
  username: string;
  displayName: string;
  avatar: string;
}

export interface SearchMessageRequest {
  /** Full-text search query */
  query?:
    | string
    | undefined;
  /** Whether to only search for pinned messages, cannot be sent with query */
  pinned?:
    | boolean
    | undefined;
  /** Maximum number of messages to fetch */
  limit?:
    | number
    | undefined;
  /** Message id before which messages should be fetched */
  before?:
    | string
    | undefined;
  /** Message id after which messages should be fetched */
  after?:
    | string
    | undefined;
  /**
   * Message sort direction
   * By default, it will be sorted by latest (default: RELEVANCE)
   */
  sort?:
    | MessageSort
    | undefined;
  /** Whether to include user (and member, if server channel) objects */
  includeUsers?: boolean | undefined;
}

export interface SearchMessageResponse {
  data?: SearchMessageResponseData | undefined;
  error?: AppError | undefined;
}

export interface SearchMessageResponseData {
  messages: Message[];
  users: APIUser[];
  members: ServerMember[];
}

function createBaseSearchUsernamesRequest(): SearchUsernamesRequest {
  return { query: "", limit: 0 };
}

export const SearchUsernamesRequest: MessageFns<SearchUsernamesRequest> = {
  encode(message: SearchUsernamesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.query !== "") {
      writer.uint32(10).string(message.query);
    }
    if (message.limit !== 0) {
      writer.uint32(16).int32(message.limit);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchUsernamesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchUsernamesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.query = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.limit = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchUsernamesRequest {
    return {
      query: isSet(object.query) ? globalThis.String(object.query) : "",
      limit: isSet(object.limit) ? globalThis.Number(object.limit) : 0,
    };
  },

  toJSON(message: SearchUsernamesRequest): unknown {
    const obj: any = {};
    if (message.query !== "") {
      obj.query = message.query;
    }
    if (message.limit !== 0) {
      obj.limit = Math.round(message.limit);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SearchUsernamesRequest>, I>>(base?: I): SearchUsernamesRequest {
    return SearchUsernamesRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SearchUsernamesRequest>, I>>(object: I): SearchUsernamesRequest {
    const message = createBaseSearchUsernamesRequest();
    message.query = object.query ?? "";
    message.limit = object.limit ?? 0;
    return message;
  },
};

function createBaseSearchUsernamesResponse(): SearchUsernamesResponse {
  return { data: undefined, error: undefined };
}

export const SearchUsernamesResponse: MessageFns<SearchUsernamesResponse> = {
  encode(message: SearchUsernamesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.data !== undefined) {
      SearchUsernamesResponseData.encode(message.data, writer.uint32(10).fork()).join();
    }
    if (message.error !== undefined) {
      AppError.encode(message.error, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchUsernamesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchUsernamesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.data = SearchUsernamesResponseData.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.error = AppError.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchUsernamesResponse {
    return {
      data: isSet(object.data) ? SearchUsernamesResponseData.fromJSON(object.data) : undefined,
      error: isSet(object.error) ? AppError.fromJSON(object.error) : undefined,
    };
  },

  toJSON(message: SearchUsernamesResponse): unknown {
    const obj: any = {};
    if (message.data !== undefined) {
      obj.data = SearchUsernamesResponseData.toJSON(message.data);
    }
    if (message.error !== undefined) {
      obj.error = AppError.toJSON(message.error);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SearchUsernamesResponse>, I>>(base?: I): SearchUsernamesResponse {
    return SearchUsernamesResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SearchUsernamesResponse>, I>>(object: I): SearchUsernamesResponse {
    const message = createBaseSearchUsernamesResponse();
    message.data = (object.data !== undefined && object.data !== null)
      ? SearchUsernamesResponseData.fromPartial(object.data)
      : undefined;
    message.error = (object.error !== undefined && object.error !== null)
      ? AppError.fromPartial(object.error)
      : undefined;
    return message;
  },
};

function createBaseSearchUsernamesResponseData(): SearchUsernamesResponseData {
  return { users: [] };
}

export const SearchUsernamesResponseData: MessageFns<SearchUsernamesResponseData> = {
  encode(message: SearchUsernamesResponseData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.users) {
      SearchUser.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchUsernamesResponseData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchUsernamesResponseData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.users.push(SearchUser.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchUsernamesResponseData {
    return {
      users: globalThis.Array.isArray(object?.users) ? object.users.map((e: any) => SearchUser.fromJSON(e)) : [],
    };
  },

  toJSON(message: SearchUsernamesResponseData): unknown {
    const obj: any = {};
    if (message.users?.length) {
      obj.users = message.users.map((e) => SearchUser.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SearchUsernamesResponseData>, I>>(base?: I): SearchUsernamesResponseData {
    return SearchUsernamesResponseData.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SearchUsernamesResponseData>, I>>(object: I): SearchUsernamesResponseData {
    const message = createBaseSearchUsernamesResponseData();
    message.users = object.users?.map((e) => SearchUser.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSearchUser(): SearchUser {
  return { id: "", username: "", displayName: "", avatar: "" };
}

export const SearchUser: MessageFns<SearchUser> = {
  encode(message: SearchUser, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.username !== "") {
      writer.uint32(18).string(message.username);
    }
    if (message.displayName !== "") {
      writer.uint32(26).string(message.displayName);
    }
    if (message.avatar !== "") {
      writer.uint32(34).string(message.avatar);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchUser {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchUser();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.username = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.displayName = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.avatar = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchUser {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      username: isSet(object.username) ? globalThis.String(object.username) : "",
      displayName: isSet(object.displayName) ? globalThis.String(object.displayName) : "",
      avatar: isSet(object.avatar) ? globalThis.String(object.avatar) : "",
    };
  },

  toJSON(message: SearchUser): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.username !== "") {
      obj.username = message.username;
    }
    if (message.displayName !== "") {
      obj.displayName = message.displayName;
    }
    if (message.avatar !== "") {
      obj.avatar = message.avatar;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SearchUser>, I>>(base?: I): SearchUser {
    return SearchUser.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SearchUser>, I>>(object: I): SearchUser {
    const message = createBaseSearchUser();
    message.id = object.id ?? "";
    message.username = object.username ?? "";
    message.displayName = object.displayName ?? "";
    message.avatar = object.avatar ?? "";
    return message;
  },
};

function createBaseSearchMessageRequest(): SearchMessageRequest {
  return {
    query: undefined,
    pinned: undefined,
    limit: undefined,
    before: undefined,
    after: undefined,
    sort: undefined,
    includeUsers: undefined,
  };
}

export const SearchMessageRequest: MessageFns<SearchMessageRequest> = {
  encode(message: SearchMessageRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.query !== undefined) {
      writer.uint32(10).string(message.query);
    }
    if (message.pinned !== undefined) {
      writer.uint32(16).bool(message.pinned);
    }
    if (message.limit !== undefined) {
      writer.uint32(24).int64(message.limit);
    }
    if (message.before !== undefined) {
      writer.uint32(34).string(message.before);
    }
    if (message.after !== undefined) {
      writer.uint32(42).string(message.after);
    }
    if (message.sort !== undefined) {
      writer.uint32(48).int32(message.sort);
    }
    if (message.includeUsers !== undefined) {
      writer.uint32(56).bool(message.includeUsers);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchMessageRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchMessageRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.query = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.pinned = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.limit = longToNumber(reader.int64());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.before = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.after = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.sort = reader.int32() as any;
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.includeUsers = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchMessageRequest {
    return {
      query: isSet(object.query) ? globalThis.String(object.query) : undefined,
      pinned: isSet(object.pinned) ? globalThis.Boolean(object.pinned) : undefined,
      limit: isSet(object.limit) ? globalThis.Number(object.limit) : undefined,
      before: isSet(object.before) ? globalThis.String(object.before) : undefined,
      after: isSet(object.after) ? globalThis.String(object.after) : undefined,
      sort: isSet(object.sort) ? messageSortFromJSON(object.sort) : undefined,
      includeUsers: isSet(object.includeUsers) ? globalThis.Boolean(object.includeUsers) : undefined,
    };
  },

  toJSON(message: SearchMessageRequest): unknown {
    const obj: any = {};
    if (message.query !== undefined) {
      obj.query = message.query;
    }
    if (message.pinned !== undefined) {
      obj.pinned = message.pinned;
    }
    if (message.limit !== undefined) {
      obj.limit = Math.round(message.limit);
    }
    if (message.before !== undefined) {
      obj.before = message.before;
    }
    if (message.after !== undefined) {
      obj.after = message.after;
    }
    if (message.sort !== undefined) {
      obj.sort = messageSortToJSON(message.sort);
    }
    if (message.includeUsers !== undefined) {
      obj.includeUsers = message.includeUsers;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SearchMessageRequest>, I>>(base?: I): SearchMessageRequest {
    return SearchMessageRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SearchMessageRequest>, I>>(object: I): SearchMessageRequest {
    const message = createBaseSearchMessageRequest();
    message.query = object.query ?? undefined;
    message.pinned = object.pinned ?? undefined;
    message.limit = object.limit ?? undefined;
    message.before = object.before ?? undefined;
    message.after = object.after ?? undefined;
    message.sort = object.sort ?? undefined;
    message.includeUsers = object.includeUsers ?? undefined;
    return message;
  },
};

function createBaseSearchMessageResponse(): SearchMessageResponse {
  return { data: undefined, error: undefined };
}

export const SearchMessageResponse: MessageFns<SearchMessageResponse> = {
  encode(message: SearchMessageResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.data !== undefined) {
      SearchMessageResponseData.encode(message.data, writer.uint32(10).fork()).join();
    }
    if (message.error !== undefined) {
      AppError.encode(message.error, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchMessageResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchMessageResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.data = SearchMessageResponseData.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.error = AppError.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchMessageResponse {
    return {
      data: isSet(object.data) ? SearchMessageResponseData.fromJSON(object.data) : undefined,
      error: isSet(object.error) ? AppError.fromJSON(object.error) : undefined,
    };
  },

  toJSON(message: SearchMessageResponse): unknown {
    const obj: any = {};
    if (message.data !== undefined) {
      obj.data = SearchMessageResponseData.toJSON(message.data);
    }
    if (message.error !== undefined) {
      obj.error = AppError.toJSON(message.error);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SearchMessageResponse>, I>>(base?: I): SearchMessageResponse {
    return SearchMessageResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SearchMessageResponse>, I>>(object: I): SearchMessageResponse {
    const message = createBaseSearchMessageResponse();
    message.data = (object.data !== undefined && object.data !== null)
      ? SearchMessageResponseData.fromPartial(object.data)
      : undefined;
    message.error = (object.error !== undefined && object.error !== null)
      ? AppError.fromPartial(object.error)
      : undefined;
    return message;
  },
};

function createBaseSearchMessageResponseData(): SearchMessageResponseData {
  return { messages: [], users: [], members: [] };
}

export const SearchMessageResponseData: MessageFns<SearchMessageResponseData> = {
  encode(message: SearchMessageResponseData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.messages) {
      Message.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.users) {
      APIUser.encode(v!, writer.uint32(18).fork()).join();
    }
    for (const v of message.members) {
      ServerMember.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchMessageResponseData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchMessageResponseData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.messages.push(Message.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.users.push(APIUser.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.members.push(ServerMember.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchMessageResponseData {
    return {
      messages: globalThis.Array.isArray(object?.messages) ? object.messages.map((e: any) => Message.fromJSON(e)) : [],
      users: globalThis.Array.isArray(object?.users) ? object.users.map((e: any) => APIUser.fromJSON(e)) : [],
      members: globalThis.Array.isArray(object?.members)
        ? object.members.map((e: any) => ServerMember.fromJSON(e))
        : [],
    };
  },

  toJSON(message: SearchMessageResponseData): unknown {
    const obj: any = {};
    if (message.messages?.length) {
      obj.messages = message.messages.map((e) => Message.toJSON(e));
    }
    if (message.users?.length) {
      obj.users = message.users.map((e) => APIUser.toJSON(e));
    }
    if (message.members?.length) {
      obj.members = message.members.map((e) => ServerMember.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SearchMessageResponseData>, I>>(base?: I): SearchMessageResponseData {
    return SearchMessageResponseData.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SearchMessageResponseData>, I>>(object: I): SearchMessageResponseData {
    const message = createBaseSearchMessageResponseData();
    message.messages = object.messages?.map((e) => Message.fromPartial(e)) || [];
    message.users = object.users?.map((e) => APIUser.fromPartial(e)) || [];
    message.members = object.members?.map((e) => ServerMember.fromPartial(e)) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
