// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.10.0
//   protoc               unknown
// source: service/v1/server_members_db.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { File } from "../../shared/v1/files.js";

export const protobufPackage = "service.v1";

export interface ServerMember {
  /** Server ID (partition key) */
  serverId: string;
  /** User ID */
  userId: string;
  /** User info (denormalized) */
  username: string;
  avatar?:
    | File
    | undefined;
  /** Member-specific */
  nickname?:
    | string
    | undefined;
  /** Time at which this user joined the server */
  joinedAt: string;
  /** Role IDs assigned to this member */
  roles: string[];
  /** Timestamp this member is timed out until */
  timeout?:
    | string
    | undefined;
  /** Voice settings */
  canPublish: boolean;
  canReceive: boolean;
}

function createBaseServerMember(): ServerMember {
  return {
    serverId: "",
    userId: "",
    username: "",
    avatar: undefined,
    nickname: undefined,
    joinedAt: "0",
    roles: [],
    timeout: undefined,
    canPublish: false,
    canReceive: false,
  };
}

export const ServerMember: MessageFns<ServerMember> = {
  encode(message: ServerMember, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.serverId !== "") {
      writer.uint32(10).string(message.serverId);
    }
    if (message.userId !== "") {
      writer.uint32(18).string(message.userId);
    }
    if (message.username !== "") {
      writer.uint32(26).string(message.username);
    }
    if (message.avatar !== undefined) {
      File.encode(message.avatar, writer.uint32(34).fork()).join();
    }
    if (message.nickname !== undefined) {
      writer.uint32(42).string(message.nickname);
    }
    if (message.joinedAt !== "0") {
      writer.uint32(48).int64(message.joinedAt);
    }
    for (const v of message.roles) {
      writer.uint32(58).string(v!);
    }
    if (message.timeout !== undefined) {
      writer.uint32(64).int64(message.timeout);
    }
    if (message.canPublish !== false) {
      writer.uint32(72).bool(message.canPublish);
    }
    if (message.canReceive !== false) {
      writer.uint32(80).bool(message.canReceive);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServerMember {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServerMember();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.serverId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.username = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.avatar = File.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.nickname = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.joinedAt = reader.int64().toString();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.roles.push(reader.string());
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.timeout = reader.int64().toString();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.canPublish = reader.bool();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.canReceive = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServerMember {
    return {
      serverId: isSet(object.serverId) ? globalThis.String(object.serverId) : "",
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      username: isSet(object.username) ? globalThis.String(object.username) : "",
      avatar: isSet(object.avatar) ? File.fromJSON(object.avatar) : undefined,
      nickname: isSet(object.nickname) ? globalThis.String(object.nickname) : undefined,
      joinedAt: isSet(object.joinedAt) ? globalThis.String(object.joinedAt) : "0",
      roles: globalThis.Array.isArray(object?.roles) ? object.roles.map((e: any) => globalThis.String(e)) : [],
      timeout: isSet(object.timeout) ? globalThis.String(object.timeout) : undefined,
      canPublish: isSet(object.canPublish) ? globalThis.Boolean(object.canPublish) : false,
      canReceive: isSet(object.canReceive) ? globalThis.Boolean(object.canReceive) : false,
    };
  },

  toJSON(message: ServerMember): unknown {
    const obj: any = {};
    if (message.serverId !== "") {
      obj.serverId = message.serverId;
    }
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.username !== "") {
      obj.username = message.username;
    }
    if (message.avatar !== undefined) {
      obj.avatar = File.toJSON(message.avatar);
    }
    if (message.nickname !== undefined) {
      obj.nickname = message.nickname;
    }
    if (message.joinedAt !== "0") {
      obj.joinedAt = message.joinedAt;
    }
    if (message.roles?.length) {
      obj.roles = message.roles;
    }
    if (message.timeout !== undefined) {
      obj.timeout = message.timeout;
    }
    if (message.canPublish !== false) {
      obj.canPublish = message.canPublish;
    }
    if (message.canReceive !== false) {
      obj.canReceive = message.canReceive;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ServerMember>, I>>(base?: I): ServerMember {
    return ServerMember.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ServerMember>, I>>(object: I): ServerMember {
    const message = createBaseServerMember();
    message.serverId = object.serverId ?? "";
    message.userId = object.userId ?? "";
    message.username = object.username ?? "";
    message.avatar = (object.avatar !== undefined && object.avatar !== null)
      ? File.fromPartial(object.avatar)
      : undefined;
    message.nickname = object.nickname ?? undefined;
    message.joinedAt = object.joinedAt ?? "0";
    message.roles = object.roles?.map((e) => e) || [];
    message.timeout = object.timeout ?? undefined;
    message.canPublish = object.canPublish ?? false;
    message.canReceive = object.canReceive ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
